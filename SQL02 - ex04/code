SELECT DISTINCT e.EMPLOYEE_ID, e.LAST_NAME
    FROM HR.EMPLOYEES e INNER JOIN 
        (SELECT DEPARTMENT_ID FROM HR.EMPLOYEES WHERE LAST_NAME LIKE '%i%') e2
    ON e.DEPARTMENT_ID = e2.DEPARTMENT_ID
ORDER BY e.EMPLOYEE_ID ASC;
